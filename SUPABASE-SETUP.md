# راه‌اندازی اتصال واقعی Supabase

## 1) ساخت پروژه
در Supabase یک پروژه جدید بسازید.

## 2) اجرای دیتابیس
فایل `database/schema.sql` را در SQL Editor اجرا کنید.

## 3) تنظیم کلید عمومی
فایل `supabase-config.js` را باز کنید و این دو مقدار را از Project Settings > API قرار دهید:
- Project URL
- Publishable key / anon key

هرگز `service_role` key را داخل این پروژه یا GitHub قرار ندهید.

## 4) اولین مدیر
بعد از ثبت‌نام اولین حساب، برای ساخت اولین سفر از RPC زیر استفاده می‌شود:
`create_trip_with_admin`

این کار حساب شما را به‌عنوان admin همان سفر ثبت می‌کند.

## 5) لینک دعوت
ادمین بعد از ورود، برای هر سفر یک token دعوت ایجاد می‌کند. لینک نمونه:
`https://YOUR-GITHUB-PAGES-URL/?join=TOKEN`

عضو ابتدا حساب می‌سازد/وارد می‌شود، سپس فرم درخواست عضویت را تکمیل می‌کند. درخواست در `membership_requests` ذخیره شده و فقط پس از تأیید ادمین، رکورد `trip_members` ساخته می‌شود.

## نکته امنیتی
توکن دعوت از طریق RPC امن `lookup_active_invite` بررسی می‌شود؛ دسترسی مستقیم عمومی به جدول دعوت‌ها داده نشده است.
