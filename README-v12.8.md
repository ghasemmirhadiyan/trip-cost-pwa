# نسخه 12.9 — ویرایش هزینه توسط مدیر

در این نسخه، مدیر سفر می‌تواند از صفحه «هزینه‌ها» هر هزینه را ویرایش کند؛ شامل عنوان، مبلغ، تاریخ، دسته‌بندی، نحوه پرداخت، پرداخت‌کننده، توضیحات و اعضای مشمول.

## مهم — اجرای SQL
در Supabase SQL Editor فایل زیر را یک‌بار اجرا کنید:

`database/migration-v12.9-expense-edit.sql`

پس از آن، فایل‌های داخل پوشه `trip-cost-pwa-v11.3` را روی GitHub Pages جایگزین کنید و با Ctrl+F5 برنامه را تازه‌سازی کنید.
