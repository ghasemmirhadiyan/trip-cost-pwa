# نسخه 12.0

- موجودی صندوق و کل هزینه‌های داشبورد از Supabase خوانده می‌شوند و مقدار نمایشی ثابت ندارند.
- صفحه صندوق نیز اطلاعات مالی واقعی را نمایش می‌دهد.
- عنوان بالای برنامه به «سفر شمال ۱۴۰۵» تغییر کرد و عبارت‌های «سفر خانوادگی» و «شمال ایران» از سربرگ حذف شدند.
- در سربرگ، برای مدیر عبارت «👑 وارد شده به عنوان مدیر سفر» نمایش داده می‌شود.
- فایل `database/reset-financial-data.sql` برای صفر کردن کامل اطلاعات مالی سفر فعلی اضافه شده است.
